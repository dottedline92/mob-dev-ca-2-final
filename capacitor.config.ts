import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MobDevCA2_FINAL',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
